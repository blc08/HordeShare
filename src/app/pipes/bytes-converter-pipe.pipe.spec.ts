import { BytesConverterPipePipe } from './bytes-converter-pipe.pipe';

describe('BytesConverterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BytesConverterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
